server {
    listen       80;
    server_name  localhost;
    root   /var/www/html/stockviva-core/public;
    index  index.php;
    #charset koi8-r;

    #access_log /dev/null;
    access_log  /var/log/dnmp/nginx.stockviva-app.access.log  main;
    error_log  /var/log/dnmp/nginx.stockviva-app.error.log  warn;

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    location / {
       try_files $uri $uri/ /index.php?$query_string;
    }

    # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    location ~ \.php$ {
       root /var/www/html/stockviva-core/public;
       fastcgi_pass php:9000;
       fastcgi_split_path_info ^(.+\.php)(/.+)$;
       fastcgi_index index.php;
       fastcgi_param REMOTE_ADDR $http_x_real_ip;
       fastcgi_param SCRIPT_FILENAME $document_root/$fastcgi_script_name;
       include fastcgi_params;
       if (!-f $document_root$fastcgi_script_name) {
           return 404;
       }
    }

    location ~ /\.(?!well-known).* {
            deny  all;
    }
}

server {
    listen       80;
    server_name  admin.stockviva.local;
    root   /var/www/html/stockviva-core/public;
    index  index.php;
    #charset koi8-r;

    #access_log /dev/null;
    access_log  /var/log/dnmp/nginx.stockviva-admin.access.log  main;
    error_log  /var/log/dnmp/nginx.stockviva-admin.error.log  warn;

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    location / {
       try_files $uri $uri/ /index.php?$query_string;
    }

    # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    location ~ \.php$ {
       root /var/www/html/stockviva-core/public;
       fastcgi_pass php:9000;
       fastcgi_split_path_info ^(.+\.php)(/.+)$;
       fastcgi_index index.php;
       fastcgi_param REMOTE_ADDR $http_x_real_ip;
       fastcgi_param SCRIPT_FILENAME $document_root/$fastcgi_script_name;
       include fastcgi_params;
       if (!-f $document_root$fastcgi_script_name) {
           return 404;
       }
    }

    location ~ /\.(?!well-known).* {
            deny  all;
    }
}